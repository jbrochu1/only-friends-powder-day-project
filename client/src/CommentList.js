export default function CommentList({currentUser, comment}){
    // const {comments} = trip
    // console.log(comments)
    return(
        <div>
            <div>
                {comment}
            </div>
        </div>
    )
}